---
// src/components/StatusWidget.astro
const status = await fetch('/api/status').then(res => res.json());
const healthy = status.ok;
---

<a href="/system-status" class={`fixed top-4 right-4 px-4 py-2 rounded text-white shadow-lg transition
  ${healthy ? 'bg-green-500 hover:bg-green-600' : 'bg-red-500 hover:bg-red-600'}`}>
  {healthy ? 'Semua Sistem Aman' : 'Ada Gangguan Sistem'}
</a>