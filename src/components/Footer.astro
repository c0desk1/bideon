---
import { SITE, SOCIALS } from '@/data/site.ts';


const currentYear = new Date().getFullYear();
---
<footer class="bg-zinc-900 border-t border-white/10 mt-16">
  <div class="container mx-auto px-4 py-8">
    <div class="flex flex-col items-center gap-4 sm:flex-row sm:justify-between sm:items-center">
      <div class="flex flex-row justify-center mt-4 sm:mt-0">
        {SOCIALS.tiktok && (
          <a href={SOCIALS.tiktok} target="_blank" class="text-zinc-400 hover:text-white ms-5 cursor-pointer">
            <i class="ri-tiktok-fill text-xl"></i>
            <span class="sr-only">TikTok account</span>
          </a>
        )}
        {SOCIALS.instagram && (
          <a href={SOCIALS.instagram} target="_blank" class="text-zinc-400 hover:text-white ms-5 cursor-pointer">
            <i class="ri-instagram-fill text-xl"></i>
            <span class="sr-only">Instagram account</span>
          </a>
        )}
        {SOCIALS.youtube && (
          <a href={SOCIALS.youtube} target="_blank" class="text-zinc-400 hover:text-white ms-5 cursor-pointer">
            <i class="ri-youtube-fill text-xl"></i>
            <span class="sr-only">YouTube account</span>
          </a>
        )}
        {SOCIALS.spotify && (
          <a href={SOCIALS.spotify} target="_blank" class="text-zinc-400 hover:text-white ms-5 cursor-pointer">
            <i class="ri-spotify-fill text-xl"></i>
            <span class="sr-only">Spotify account</span>
          </a>
        )}
      </div>
      <span class="text-sm text-zinc-400 text-center sm:text-left">
        © {currentYear} <a href="#" class="cursor-pointer hover:text-zinc-100">{SITE.TITLE}</a>. All Rights Reserved.
      </span>
    </div>
  </div>
</footer>